---
layout: none
permalink: /data/
---
[
  {% for section in site.data.toc %}
  {
    "section": {{ section[0] | jsonify }},
    "label": {{ section[1]['label'] | jsonify }},
    "areas": [
      {% for principle in section[1].principles %}
      {
        "slug": {{ principle['label'] | slugify | jsonify }},
        "label": {{ principle['label'] | jsonify }},
        "standards": [
          {% for string in principle.standards %}
            {% assign slug = string | split: '/' | slice: 1 | first %}
            {% assign standard = site.standards | where: 'slug', slug | first %}
            {
              "slug": {{ slug | jsonify }},
              "title": {{ standard['evaluationName'] | jsonify }},
              "category": {{ principle['label'] | jsonify }},
              "status": {{ standard['readinessFlag'] | jsonify }},
              "criteria": [
                {% for criteria in standard.criteria %}
                  {
                    "summary": {{ criteria['criteria_summary'] | jsonify }},
                    "indicators": [
                      {% for item in criteria.indicators %}
                        {
                          "indicator": {{ item['indicator'] | jsonify}},
                          "procedure": {{ item['procedure_html'] | jsonify}}
                        }{% unless forloop.last %},{% endunless %}
                      {% endfor %}
                    ]
                  }{% unless forloop.last %},{% endunless %}
                {% endfor %}
              ]
            }{% unless forloop.last %},{% endunless %}
          {% endfor %}
        ]
      }{% unless forloop.last %},{% endunless %}
      {% endfor %}
    ]
  }{% unless forloop.last %},{% endunless %}
  {% endfor %}
]